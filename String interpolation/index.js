const number = 42 ;
const message = `this is ${number}`;

console.log(message);

const greeting = 'Hello';
const who = 'World';

const mess = `${greeting}, ${who}!`;
console.log(mess); // => 'Hello, World!'